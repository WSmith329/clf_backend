# -- FILE: pytest.ini (or tox.ini)
[pytest]
DJANGO_SETTINGS_MODULE = clf_backend.settings
python_files = test_*.py
norecursedirs = lib bin
pythonpath =
    ./src
    ./tests/config
django_find_project = false
addopts =
    --reuse-db
